import React from "react";
import { SvgIcon } from "@material-ui/core";

export const CustomIcon = (props: any) => {
  return (
    <SvgIcon {...props} component={props.component} viewBox="0 0 2600 852" />
  );
};
